AIRFLOW_UID=197609
import os
from pathlib import Path

class Config:
    # MinIO
    MINIO_ENDPOINT = os.getenv('MINIO_ENDPOINT', 'minio-tai:9000')
    MINIO_ACCESS_KEY = os.getenv('MINIO_ACCESS_KEY', 'minio')
    MINIO_SECRET_KEY = os.getenv('MINIO_SECRET_KEY', 'minio123')
    MINIO_BUCKET = os.getenv('MINIO_BUCKET', 'pdf-bucket')
    
    # Kafka
    KAFKA_BOOTSTRAP_SERVERS = os.getenv('KAFKA_BOOTSTRAP_SERVERS', 'kafka-tai:9091')
    KAFKA_TOPIC = os.getenv('KAFKA_TOPIC', 'minio-events')
    
    # Embeddings
    EMBEDDING_MODEL = os.getenv('EMBEDDING_MODEL', 'sentence-transformers/all-MiniLM-L6-v2')
    # Alternative avec Ollama
    # EMBEDDING_MODEL = os.getenv('EMBEDDING_MODEL', 'nomic-embed-text')
    # OLLAMA_HOST = os.getenv('OLLAMA_HOST', 'http://ollama:11434')
    
    # Chunking
    CHUNK_SIZE = int(os.getenv('CHUNK_SIZE', '500'))
    CHUNK_OVERLAP = int(os.getenv('CHUNK_OVERLAP', '50'))
    
    # Vector Store (Qdrant)
    QDRANT_HOST = os.getenv('QDRANT_HOST', 'qdrant')
    QDRANT_PORT = int(os.getenv('QDRANT_PORT', '6333'))
    QDRANT_COLLECTION = os.getenv('QDRANT_COLLECTION', 'documents')
    
    # Paths
    TEMP_DIR = Path('/tmp/rag_pipeline')
    TEMP_DIR.mkdir(exist_ok=True)